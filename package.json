{
  "name": "millenium-search-worker",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "prisma generate",
    "postinstall": "prisma generate",
    "dev": "bun --watch src/index.ts",
    "start": "bun src/index.ts",
    "sync": "bun src/sync.ts",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@prisma/client": "^6.2.0",
    "meilisearch": "^0.42.0",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "prisma": "^6.2.0",
    "typescript": "^5.6.3"
  }
}
